// ============================================================================
// Render — calendar section. Phase TS-12 (skeleton). Body still inline.
// ============================================================================
import type { SectionId } from './types';
export const CALENDAR_SECTION: SectionId = 'calendar';
