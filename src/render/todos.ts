// ============================================================================
// Render — todos section. Phase TS-12 (skeleton). Body still inline.
// ============================================================================
import type { SectionId } from './types';
export const TODOS_SECTION: SectionId = 'todos';
