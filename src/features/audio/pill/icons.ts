// ============================================================================
// Audio pill — play/pause SVG icons. Phase TS-20.
//
// Inlined SVG strings (same as the legacy bare globals PLAY_ICON_SVG /
// PAUSE_ICON_SVG). The catalogue + detail pill widgets reference these
// directly; the mini-player uses the broader `icon()` registry from
// /src/lib/render-utils.
// ============================================================================

export const PLAY_ICON_SVG =
  '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M8 5v14l11-7z"/></svg>';

export const PAUSE_ICON_SVG =
  '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M6 5h4v14H6zm8 0h4v14h-4z"/></svg>';
